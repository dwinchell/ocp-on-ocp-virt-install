# No real inventory needed: provisioning, ISO generation, and the install
# wait-loop all run against the management (hub) cluster's API from
# localhost. The nested cluster's own nodes are never targeted directly by
# Ansible — the agent-based installer handles the OS install itself once
# the VMs boot the ISO.
all:
  hosts:
    localhost:
      ansible_connection: local
